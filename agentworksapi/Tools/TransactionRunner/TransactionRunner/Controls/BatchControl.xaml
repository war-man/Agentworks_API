<UserControl x:Class="TransactionRunner.Controls.BatchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TransactionRunner.Controls"
             mc:Ignorable="d" 
             d:DesignWidth="300"
             d:DesignHeight="30"
             Initialized="BatchControl_Initialized">
    <Grid>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Label Name="LblBatchAmount" Grid.Column="0" Content="Batch Amount:" HorizontalAlignment="Left" Margin="15,0,0,0" VerticalAlignment="Top" Width="159" Height="30"/>
            <TextBox Name="TxtBatchAmount" Grid.Column="1" HorizontalAlignment="Stretch" Margin="0,1,0,1" TextWrapping="NoWrap" Text="{Binding BatchNumber}" IsEnabled="{Binding Enabled}" VerticalAlignment="Top" Height="24" TextAlignment="Left" PreviewTextInput="NumberValidationTextBox"/>
        </Grid>
        <Rectangle Fill="White" Opacity="0.7" Visibility="{Binding ModalVisibility}"></Rectangle>
    </Grid>
</UserControl>
